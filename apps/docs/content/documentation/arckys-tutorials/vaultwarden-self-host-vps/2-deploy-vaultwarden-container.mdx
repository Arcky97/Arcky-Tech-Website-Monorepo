## Deploy the Vaultwarden container (on Portainer)

- Go to Portainer 
  <MediaGallery
    items={[
      {
        src: "documentation/arcky-tutorials/discord-bot-tutorial/Portainer Dashboard.png",
        alt: "Portainer Dashboard",
        width: 1916,
        height: 908
      }
    ]}
    size="large"
  />

- Click "Stacks" in the Side menu 
  <MediaGallery
    items={[
      {
        src: "documentation/arcky-tutorials/discord-bot-tutorial/Portainer Stacks.png",
        alt: "Portainer Stacks",
        width: 253,
        height: 354
      }
    ]}
    size="small"
  />

- Click "Add Stack" 
  <MediaGallery
    items={[
      {
        src: "documentation/arcky-tutorials/discord-bot-tutorial/Portainer Add Stack.png",
        alt: "Portainer Add Stack",
        width: 1598,
        height: 283
      }
    ]}
    size="medium"
  />

- Fill in Name and content 
  <MediaGallery
    items={[
      {
        src: "documentation/arcky-tutorials/discord-bot-tutorial/Portainer Stack Name and Content.png",
        alt: "Portainer Stack Name and Content",
        width: 1586,
        height: 868
      }
    ]}
    size="large"
  />

```nginx:Name 
vaultwarden
```

```yaml:Content
version: "3.9"

services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    ports:
      - "127.0.0.1:8222:80"
    volumes:
      - /opt/vaultwarden/data:/data
    environment:
      DOMAIN: https://vault.your-domain.com
      SIGNUPS_ALLOWED: false
      WEBSOCKET_ENABLED: true
      ADMIN_TOKEN: CHANGE_ME
      LOG_LEVEL: warn
```
Before deploying the stack, generate the admin token in SSH:
```bash
openssl rand -base64 48
```

- replace "your-domain" in `https://vault.your-domain.com` with your domain. The `vault.` part is the sub domain name, you can change this too if you like.
- replace `CHANGE_ME` with the generated token and **deploy the stack**
- deploy the stack